step 1(optional): if you're working with ASCII brainvision files: go through the "Sleep_staging.ipynb" notebook to convert the files to a workable state and optionally filter out relevant electrodes also allows the brainvision files to be downsampled to save disk space and speed up analysis

step 2(optional): use the edf conversion in the sleep_staging notebook to convert the resulting files to edf to score any unscored data with u-sleep

step 3: